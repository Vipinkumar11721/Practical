# Copy this file to `.env` and fill values (DO NOT commit real secrets)

# MongoDB Atlas connection string (mongodb+srv://<user>:<password>@cluster0.../listings?retryWrites=true&w=majority)
ATLUSDB_URL=

# Session secret
SECRET=

# Cloudinary credentials (for image uploads)
CLOUD_NAME=
CLOUD_API_KEY=
CLOUD_API_SECRET=

# Optional: port (Render sets its own PORT automatically)
PORT=8080
